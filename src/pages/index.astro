---
import Layout from "../layouts/Layout.astro";
import LandingSplitScreen from "../components/shared/LandingSplitScreen";
import LoadingScreen from "../components/shared/LoadingScreen.astro";
import andreaImg from "../assets/academia/Andrea_NoBg_11zon.webp";
import maeImg from "../assets/fproductions/Mae_NoBg_11zon.webp";
---

<Layout title="Felinas — Elige tu lado">
  <LoadingScreen />
  <LandingSplitScreen client:load andreaSrc={andreaImg.src} maeSrc={maeImg.src} />
</Layout>

<style is:global>
  html, body {
    overflow: hidden !important;
    height: 100%;
    background: #000;
  }
</style>

<script>
  window.addEventListener("load", () => {
    const loader = document.getElementById("loading-screen");
    if (!loader) return;
    loader.style.transition = "opacity 0.4s ease";
    loader.style.opacity = "0";
    setTimeout(() => loader.remove(), 400);
  });
</script>
